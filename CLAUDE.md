# CLAUDE.md - cc_tapgame 開発ルール

## 基本ルール

- **言語**: すべてのチャット応答は日本語で行うこと
- **タイムゾーン**: 日本時間 (JST / UTC+9) を基準とすること。日時を扱うコードではすべて `Asia/Tokyo` を使用する
- **変更時は必ず自動テストコードを合わせて作成すること**

## プロジェクト概要

- **プロジェクト名**: cc_tapgame (タップゲーム)
- **リポジトリ**: sakanayuki/cc_tapgame

## ログ出力の統一ルール

すべてのシステム構成で以下のログルールを適用する。

### 共通フォーマット

```
[YYYY-MM-DDTHH:mm:ss.SSS+09:00] [LEVEL] [MODULE] MESSAGE {context}
```

- **タイムスタンプ**: ISO 8601 形式、JST (UTC+9)
- **LEVEL**: `DEBUG` / `INFO` / `WARN` / `ERROR` / `FATAL` の5段階
- **MODULE**: ログ出力元のモジュール名 (例: `GameEngine`, `API`, `Auth`)
- **MESSAGE**: ログメッセージ本文
- **context**: 任意の構造化データ (JSON形式)

### ログレベルの使い分け

| レベル | 用途 |
|--------|------|
| `DEBUG` | 開発時のデバッグ情報。本番環境では出力しない |
| `INFO` | 正常な動作の記録 (起動、終了、主要な処理完了) |
| `WARN` | 異常ではないが注意が必要な状態 |
| `ERROR` | エラー発生。処理は継続可能 |
| `FATAL` | 致命的エラー。アプリケーション停止が必要 |

### 環境別設定

| 環境 | 最小レベル | 出力先 |
|------|-----------|--------|
| 開発 (development) | `DEBUG` | コンソール + ファイル |
| ステージング (staging) | `INFO` | コンソール + ファイル |
| 本番 (production) | `INFO` | ファイル + 外部サービス |

### ログ出力のルール

- **個人情報・機密情報は絶対にログに含めない** (パスワード、トークン、メールアドレス等)
- エラーログには必ずスタックトレースを含める
- リクエスト処理には一意の `requestId` を付与して追跡可能にする
- ログファイルは日次でローテーションする

### フロントエンド (ブラウザ)

```typescript
// フロントエンドでは console ラッパーを使用
logger.info('GameEngine', 'ゲーム開始', { userId: 'xxx', stage: 1 });
logger.error('API', 'サーバー通信エラー', { endpoint: '/api/score', status: 500 });
```

### バックエンド (サーバー)

```typescript
// バックエンドでは構造化ログライブラリを使用
logger.info('API', 'スコア保存完了', { requestId: 'abc-123', userId: 'xxx', score: 100 });
logger.error('DB', 'データベース接続エラー', { error: err.message, stack: err.stack });
```

## テストルール

- **すべての機能追加・バグ修正にはテストコードを必ず作成すること**
- テストファイルは対象ファイルと同じディレクトリに `*.test.ts` または `*.spec.ts` の命名で配置する
- テストは以下の3種類を必要に応じて作成する:
  - **ユニットテスト**: 個々の関数・クラスの動作検証
  - **統合テスト**: モジュール間の連携検証
  - **E2Eテスト**: ユーザー操作のシナリオ検証
- テストカバレッジは80%以上を目標とする
- テスト実行コマンド: `npm test`

## コーディング規約

- TypeScript を使用し、`strict` モードを有効にする
- 変数・関数名は camelCase、クラス名は PascalCase、定数は UPPER_SNAKE_CASE
- コードの意図が明確でない箇所にはコメントを付ける (自明なコードにはコメント不要)
- 未使用のコード・変数は残さず削除する

## ブランチ運用

- 機能開発: `feature/<機能名>`
- バグ修正: `fix/<バグ名>`
- Claude Code 自動開発: `claude/<タスク名>-<セッションID>`
